import { render, screen } from "@testing-library/react";

import App from "./App";

describe("header renders with correct text", () => {
  beforeEach(() => {
    render(<App />);
  });

  test("", () => {});
});
